import {
  wrapper,
  marginTop,
} from '../stylesheets/Common.module.css';

const TypesPage = () => (
  <div
    className={`${wrapper} ${marginTop}`}
  >
    TypesPage
  </div>
);

export default TypesPage;
