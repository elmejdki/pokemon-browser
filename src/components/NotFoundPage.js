import {
  wrapper,
  marginTop,
} from '../stylesheets/Common.module.css';

const NotFoundPage = () => (
  <div
    className={`${wrapper} ${marginTop}`}
  >
    NotFoundPage
  </div>
);

export default NotFoundPage;
