import {
  wrapper,
  marginTop,
} from '../stylesheets/Common.module.css';

const PokemonPage = () => (
  <div
    className={`${wrapper} ${marginTop}`}
  >
    PokemonPage
  </div>
);

export default PokemonPage;
